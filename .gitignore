# Environment / secrets
.env
.env
.env-*
.env copy
*.env

# OS / editor cruft
.DS_Store

# Build / cache artefacts
__pycache__/
*.py[cod]
*.egg-info/
.venv*/
venv/
dist/
release.tar.gz
.cursorignore
.cache/
data/*.db
data/*.db-journal
data/lookup_metrics.jsonl
data/lookup_metrics.jsonl.tmp
data/pending_cache_writes.jsonl
data/pending_cache_writes.jsonl.tmp
data/imports/**/*.csv


redis-cache-export.*
redis-dump.rdb

# Large generated archives that aren't useful to the assistant
*.zip

# Deployment secrets
deploy/gcp-sa-key.json
*-sa-key.json
